import { Container, SectionDark } from './common';

export const ProjectsSection = () => {
  return (
    <SectionDark>
      <Container>
        <h2>{`<projects>`}</h2>
        <h2>{`</projects>`}</h2>
      </Container>
    </SectionDark>
  );
};
